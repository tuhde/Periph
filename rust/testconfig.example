# Copy to rust/testconfig and fill in your values.
# testconfig is gitignored — never commit it.

# Linux I2C bus number (e.g. 1 for /dev/i2c-1)
I2C_BUS=1

# Device I2C address in hex (optional — falls back to chip_defaults)
# I2C_ADDR=0x40
