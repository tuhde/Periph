/// Translation unit for the DHT11 driver. The driver is template-based, so all
/// logic lives in the header file. This .cpp file exists to satisfy build
/// systems that require a source file alongside the header and to give the
/// linker a place to anchor platform-specific time symbols if needed.
