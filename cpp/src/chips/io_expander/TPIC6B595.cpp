// TPIC6B595 is a header-only template driver — implementations live in
// TPIC6B595.h alongside the template definitions. This .cpp file exists
// so the chip can be added to a Zephyr module / ESP-IDF component without
// picking up the Arduino-specific symbols from a future translation unit.
#include "TPIC6B595.h"
