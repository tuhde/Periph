# Chip Spec: INA226

**Manufacturer:** Texas Instruments  
**Datasheet:** `datasheets/power/ina226.pdf`  
**Category:** `power`  
**Transports:** I²C, SMBus

## Overview

The INA226 is a 36V, 16-bit current, voltage, and power monitor. It measures the differential shunt voltage across IN+ and IN− to derive current, and the bus voltage at VBUS. An internal multiplier calculates power. Current and power readings require the Calibration Register to be programmed with the shunt resistor value and desired current LSB. The device operates from 2.7–5.5 V and supports up to 16 I²C addresses via A0/A1 pins.

## Transport Configuration

### I²C / SMBus

- **Addresses:** `0x40`–`0x4F` (16 addresses, set by A0 and A1 pins)

| A1  | A0  | Address |
|-----|-----|---------|
| GND | GND | `0x40`  |
| GND | VS  | `0x41`  |
| GND | SDA | `0x42`  |
| GND | SCL | `0x43`  |
| VS  | GND | `0x44`  |
| VS  | VS  | `0x45`  |
| VS  | SDA | `0x46`  |
| VS  | SCL | `0x47`  |
| SDA | GND | `0x48`  |
| SDA | VS  | `0x49`  |
| SDA | SDA | `0x4A`  |
| SDA | SCL | `0x4B`  |
| SCL | GND | `0x4C`  |
| SCL | VS  | `0x4D`  |
| SCL | SDA | `0x4E`  |
| SCL | SCL | `0x4F`  |

- **Max clock:** 400 kHz (fast mode), 2.94 MHz (high-speed mode)
- **SMBus timeout:** 28 ms — the interface resets if SCL is held low longer than this

All registers are 16-bit, big-endian (MSB first). Read/write is via a register pointer byte sent before data.

## Register Map

| Address | Name            | R/W | Reset    | Description |
|---------|-----------------|-----|----------|-------------|
| `0x00`  | Configuration   | R/W | `0x4127` | Mode, conversion times, averaging |
| `0x01`  | Shunt Voltage   | R   | `0x0000` | Differential shunt voltage (2's complement) |
| `0x02`  | Bus Voltage     | R   | `0x0000` | Bus voltage at VBUS pin |
| `0x03`  | Power           | R   | `0x0000` | Calculated power (requires calibration) |
| `0x04`  | Current         | R   | `0x0000` | Calculated current (requires calibration) |
| `0x05`  | Calibration     | R/W | `0x0000` | Shunt value and current LSB scaling |
| `0x06`  | Mask/Enable     | R/W | `0x0000` | Alert configuration and status flags |
| `0x07`  | Alert Limit     | R/W | `0x0000` | Alert threshold value |
| `0xFE`  | Manufacturer ID | R   | `0x5449` | Texas Instruments ID |
| `0xFF`  | Die ID          | R   | `0x2260` | Die revision ID |

### Bit Fields

#### `Configuration` (`0x00`) — reset `0x4127`

| Bits  | Name   | Description |
|-------|--------|-------------|
| 15    | RST    | Write 1 to reset all registers; self-clears |
| 14:12 | —      | Reserved |
| 11:9  | AVG    | Averaging count (see Table below) |
| 8:6   | VBUSCT | Bus voltage conversion time |
| 5:3   | VSHCT  | Shunt voltage conversion time |
| 2:0   | MODE   | Operating mode |

**AVG — number of averages:**

| AVG | Samples |
|-----|---------|
| 000 | 1 (default) |
| 001 | 4 |
| 010 | 16 |
| 011 | 64 |
| 100 | 128 |
| 101 | 256 |
| 110 | 512 |
| 111 | 1024 |

**VBUSCT / VSHCT — conversion time (same encoding):**

| Value | Time |
|-------|------|
| 000   | 140 µs |
| 001   | 204 µs |
| 010   | 332 µs |
| 011   | 588 µs |
| 100   | 1.1 ms (default) |
| 101   | 2.116 ms |
| 110   | 4.156 ms |
| 111   | 8.244 ms |

**MODE:**

| Value | Mode |
|-------|------|
| 000   | Power-down |
| 001   | Shunt voltage, triggered |
| 010   | Bus voltage, triggered |
| 011   | Shunt and bus, triggered |
| 100   | Power-down |
| 101   | Shunt voltage, continuous |
| 110   | Bus voltage, continuous |
| 111   | Shunt and bus, continuous (default) |

#### `Shunt Voltage` (`0x01`)

| Bits  | Name | Description |
|-------|------|-------------|
| 15    | SIGN | Sign bit (2's complement) |
| 14:0  | SD   | Shunt voltage magnitude |

#### `Mask/Enable` (`0x06`)

| Bit | Name | Description |
|-----|------|-------------|
| 15  | SOL  | Alert on shunt voltage over-limit |
| 14  | SUL  | Alert on shunt voltage under-limit |
| 13  | BOL  | Alert on bus voltage over-limit |
| 12  | BUL  | Alert on bus voltage under-limit |
| 11  | POL  | Alert on power over-limit |
| 10  | CNVR | Alert on conversion ready |
| 4   | AFF  | Alert Function Flag (read-only) |
| 3   | CVRF | Conversion Ready Flag (read-only) |
| 2   | OVF  | Math Overflow Flag (read-only) |
| 1   | APOL | Alert polarity: 0 = active-low (default), 1 = active-high |
| 0   | LEN  | Alert latch: 0 = transparent (default), 1 = latch |

## Initialization Sequence

### Minimal

1. Write Calibration Register (`0x05`) — required for current and power readings:
   ```
   Current_LSB = max_current / 32768
   CAL = int(0.00512 / (Current_LSB × r_shunt))
   ```
2. Device is already in continuous shunt+bus mode at power-on; no further configuration needed.

### Full

1. Write Configuration Register (`0x00`) with desired averaging, conversion times, and mode.
2. Write Calibration Register (`0x05`) as above.
3. Optionally configure Mask/Enable (`0x06`) and Alert Limit (`0x07`) registers.

## Implementation Stages

### Minimal

Goal: read bus voltage, shunt voltage, current, and power with no configuration beyond the transport and shunt resistor.

Constructor parameters: `transport`, `r_shunt` (Ω, default `0.1`), `max_current` (A, default `2.0`).

Constructor computes and writes the Calibration Register automatically.

**Baked-in defaults (written to Configuration Register at init):**
- MODE = 111 (shunt + bus, continuous)
- VBUSCT = 100 (1.1 ms)
- VSHCT = 100 (1.1 ms)
- AVG = 000 (1 sample, no averaging)

| Operation        | Parameters | Returns | Notes |
|------------------|------------|---------|-------|
| `voltage()`      | —          | float   | Bus voltage in V |
| `shunt_voltage()`| —          | float   | Shunt voltage in V |
| `current()`      | —          | float   | Current in A (signed) |
| `power()`        | —          | float   | Power in W |

### Full

Extends Minimal. Adds full register access.

| Operation             | Parameters                              | Returns | Notes |
|-----------------------|-----------------------------------------|---------|-------|
| *(inherits Minimal)*  | | | |
| `configure()`         | `avg`, `vbus_ct`, `vsh_ct`, `mode`      | —       | Write Configuration Register |
| `conversion_ready()`  | —                                       | bool    | Read CVRF bit from Mask/Enable |
| `overflow()`          | —                                       | bool    | Read OVF bit from Mask/Enable |
| `set_alert()`         | `function`, `limit`, `polarity`, `latch`| —       | Write Mask/Enable + Alert Limit |
| `alert_flags()`       | —                                       | int     | Read Mask/Enable register |
| `reset()`             | —                                       | —       | Set RST bit; re-writes calibration afterward |
| `shutdown()`          | —                                       | —       | Set MODE = 000 |
| `wake()`              | —                                       | —       | Restore previous MODE |
| `manufacturer_id()`   | —                                       | int     | Read `0xFE`; expect `0x5449` |
| `die_id()`            | —                                       | int     | Read `0xFF`; expect `0x2260` |

**Alert function values for `set_alert(function, ...)`:**

| Value | Function |
|-------|----------|
| `SOL` | Shunt voltage over-limit |
| `SUL` | Shunt voltage under-limit |
| `BOL` | Bus voltage over-limit |
| `BUL` | Bus voltage under-limit |
| `POL` | Power over-limit |
| `CNVR`| Conversion ready |

The `limit` parameter for `set_alert` is in natural units (V for voltage functions, W for power).

## Data Conversion

```
Current_LSB = max_current / 32768            # A/bit
CAL         = int(0.00512 / (Current_LSB × r_shunt))

shunt_voltage (V) = raw_s16 × 2.5e-6        # raw is signed 16-bit
bus_voltage   (V) = raw_u16 × 1.25e-3       # raw is unsigned 16-bit
current       (A) = raw_s16 × Current_LSB   # raw is signed 16-bit
power         (W) = raw_u16 × 25 × Current_LSB
```

For `set_alert` limit conversion:
```
# Shunt voltage limit (SOL/SUL):
alert_raw = int(limit_V / 2.5e-6)

# Bus voltage limit (BOL/BUL):
alert_raw = int(limit_V / 1.25e-3)

# Power limit (POL):
alert_raw = int(limit_W / (25 × Current_LSB))
```

## Timing Constraints

- Recovery from power-down mode: 40 µs
- All registers are volatile — reprogrammed on every power-up
- Conversion Ready Flag (CVRF) clears on write to Configuration Register or on read of Mask/Enable Register

## Implementation Notes

- Current Register (`0x04`) and Power Register (`0x03`) read as zero until Calibration Register is programmed with a non-zero value
- Current Register is signed (2's complement); Power Register is unsigned
- Only one alert function can be active at a time; if multiple bits are set in Mask/Enable, the highest-priority bit (MSB) wins
- AFF bit clears automatically in transparent mode (LEN=0) once the fault clears; in latch mode (LEN=1) it clears only when Mask/Enable is read
- Reading Mask/Enable Register clears the CVRF bit — read it last if checking both flags
- The register pointer persists between transactions; consecutive reads of the same register do not need to re-send the pointer

## Implementation Checklist

Tick each box as the item is committed. The PR may not be opened until every box is ticked.

### Python
- [x] Driver `python/periph/chips/power/ina226.py` — Google-style docstring on every class and public method
- [ ] Examples `python/examples/power/ina226/minimal.py` — Tier-1 signature comment on every call
- [ ] Examples `python/examples/power/ina226/complete.py` — Tier-1 + Tier-2
- [ ] Examples `python/examples/power/ina226/demo.py` — Tier-1 + Tier-3
- [x] Tests `python/tests/power/ina226_test.py` (MicroPython)
- [x] Tests `python/tests/power/ina226_test_cp.py` (CircuitPython)
- [x] Tests `python/tests/power/ina226_test_linux.py` (Linux)

### C++
- [x] Driver `cpp/src/chips/power/INA226.h` — Doxygen `/** @brief */` on every class and public method
- [x] Driver `cpp/src/chips/power/INA226.cpp`
- [ ] Examples `cpp/examples/INA226_Minimal/INA226_Minimal.ino` — Tier-1
- [ ] Examples `cpp/examples/INA226_Complete/INA226_Complete.ino` — Tier-1 + Tier-2
- [ ] Examples `cpp/examples/INA226_Demo/INA226_Demo.ino` — Tier-1 + Tier-3
- [ ] Examples `cpp/examples/INA226_Minimal_Zephyr/src/main.cpp` — Tier-1
- [ ] Examples `cpp/examples/INA226_Complete_Zephyr/src/main.cpp` — Tier-1 + Tier-2
- [ ] Examples `cpp/examples/INA226_Demo_Zephyr/src/main.cpp` — Tier-1 + Tier-3
- [x] Tests `cpp/tests/power/ina226_test/ina226_test.ino` (Arduino)
- [x] Tests `cpp/tests/power/ina226_test_linux/ina226_test_linux.cpp` (Linux GCC)
- [x] Tests `cpp/tests/power/ina226_test_zephyr/src/main.cpp` (Zephyr)

### Node.js
- [x] Driver `nodejs/packages/periph/src/chips/power/ina226.js` — JSDoc on every class and exported method
- [ ] Examples `nodejs/packages/periph/examples/power/ina226/minimal.js` — Tier-1
- [ ] Examples `nodejs/packages/periph/examples/power/ina226/complete.js` — Tier-1 + Tier-2
- [ ] Examples `nodejs/packages/periph/examples/power/ina226/demo.js` — Tier-1 + Tier-3
- [x] Tests `nodejs/tests/power/ina226_test.js`

### Node-RED
- [x] Node runtime `nodejs/packages/node-red-contrib-periph-power/nodes/ina226/ina226.js`
- [x] Node editor `nodejs/packages/node-red-contrib-periph-power/nodes/ina226/ina226.html` — `data-help-name` section with inputs, outputs, and config description
- [x] Demo flow `nodejs/packages/node-red-contrib-periph-power/examples/ina226/demo.json` — tab `info` field describes the scenario

### Rust
- [x] Driver `rust/periph/src/chips/power/ina226.rs` — `//!` module doc + `///` on every `pub` item
- [ ] Examples `rust/examples/ina226_minimal/src/main.rs` — Tier-1
- [ ] Examples `rust/examples/ina226_complete/src/main.rs` — Tier-1 + Tier-2
- [ ] Examples `rust/examples/ina226_demo/src/main.rs` — Tier-1 + Tier-3
- [x] Tests `rust/tests/power/ina226_test/src/main.rs` (Linux)
- [x] Tests `rust/tests/power/ina226_test_esp32s3/src/main.rs` (ESP32-S3)

### JVM (Java)
- [x] Driver `jvm/periph-java/src/main/java/it/uhde/periph/chips/power/Ina226Minimal.java`
- [x] Driver `jvm/periph-java/src/main/java/it/uhde/periph/chips/power/Ina226Full.java`
- [x] Examples `jvm/examples/java/power/ina226/Minimal.java` — JBang, Tier-1
- [x] Examples `jvm/examples/java/power/ina226/Complete.java` — JBang, Tier-1 + Tier-2
- [x] Examples `jvm/examples/java/power/ina226/Demo.java` — JBang, Tier-1 + Tier-3
- [x] Tests `jvm/tests/power/ina226/Ina226Test.java` — JBang integration test (Pi hardware)

### JVM (Kotlin)
- [x] Driver `jvm/periph-kotlin/src/main/kotlin/it/uhde/periph/chips/power/Ina226Minimal.kt`
- [x] Driver `jvm/periph-kotlin/src/main/kotlin/it/uhde/periph/chips/power/Ina226Full.kt`
- [x] Examples `jvm/examples/kotlin/power/ina226/Minimal.kt` — JBang, Tier-1
- [x] Examples `jvm/examples/kotlin/power/ina226/Complete.kt` — JBang, Tier-1 + Tier-2
- [x] Examples `jvm/examples/kotlin/power/ina226/Demo.kt` — JBang, Tier-1 + Tier-3
- [x] Tests `jvm/tests/power/ina226/Ina226Test.kt` — JBang integration test (Pi hardware)

### JVM (Groovy)
- [x] Driver `jvm/periph-groovy/src/main/groovy/it/uhde/periph/chips/power/Ina226Minimal.groovy`
- [x] Driver `jvm/periph-groovy/src/main/groovy/it/uhde/periph/chips/power/Ina226Full.groovy`
- [x] Examples `jvm/examples/groovy/power/ina226/Minimal.groovy` — JBang, Tier-1
- [x] Examples `jvm/examples/groovy/power/ina226/Complete.groovy` — JBang, Tier-1 + Tier-2
- [x] Examples `jvm/examples/groovy/power/ina226/Demo.groovy` — JBang, Tier-1 + Tier-3
- [x] Tests `jvm/tests/power/ina226/Ina226Test.groovy` — JBang integration test (Pi hardware)
