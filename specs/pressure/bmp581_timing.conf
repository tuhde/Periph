# Machine-readable mirror of specs/pressure/bmp581.md's Timing Constraints
# section. See specs/testing_framework.md, "Conformance Implementation".

# Soft reset: from CMD=0xB6 write to STATUS.nvm_rdy=1 after reset,
# <= 2 ms max per datasheet.
soft_reset_max_ms=2
soft_reset_samplerate=1m
soft_reset_capture_ms=10