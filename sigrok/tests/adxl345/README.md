# No captured `.sr` session is committed yet for ADXL345.

Open PulseView, attach the `i2c` decoder plus the `adxl345` protocol
decoder on top, run any of the example scripts against real hardware, and
save the capture here as `ADXL345-Test.sr` for manual verification of the
annotations.