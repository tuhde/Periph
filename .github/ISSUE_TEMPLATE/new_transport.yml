name: New Transport
description: Request a new transport implementation
title: "transport: <TransportName>"
labels: ["transport", "needs-spec"]
body:
  - type: input
    id: transport_name
    attributes:
      label: Transport name
      placeholder: e.g. SPI, I²C, UART, CAN
    validations:
      required: true

  - type: checkboxes
    id: platforms
    attributes:
      label: Target platforms
      options:
        - label: "Python — MicroPython"
        - label: "Python — CircuitPython"
        - label: "Python — Linux (smbus2 / spidev)"
        - label: "C++ — Arduino"
        - label: "C++ — Zephyr RTOS"
        - label: "C++ — Linux (GCC)"
        - label: "Node.js — Linux"
        - label: "Rust — embedded-hal (bare-metal / ESP32-S3)"
        - label: "Rust — Linux (spidev / i2c-dev)"
        - label: Other (describe in notes)

  - type: textarea
    id: notes
    attributes:
      label: Notes
      description: Protocol reference, specific hardware constraints, or other relevant details.
